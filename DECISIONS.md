# 已确认决策

## D-001：Hub 只读原始会话源

- 决定：所有厂商数据库和 transcript 只读；用户自己的收藏、备注和项目状态写入 Hub 自有数据。
- 原因：避免破坏厂商应用不公开的生命周期和 schema 约束。
- 重评条件：厂商提供官方、事务安全的写入 API，且用户明确需要。

## D-002：只绑定 localhost，模型不是依赖

- 决定：服务只绑定 `127.0.0.1`；默认路径零云端模型调用。
- 原因：会话包含私密代码和个人信息；搜索和回顾必须离线可用。
- 重评条件：仅可增加默认关闭、用户主动配置的可选增强。

## D-003：续接能力必须诚实分级

- 决定：严格区分 `session`、`command`、`workspace`、`client`、`none`。
- 原因：打开应用或工作区不等于恢复精确会话。
- 重评条件：对应产品提供经过验证的官方深链或 CLI resume。

## D-004：Hub 管回忆，仓库管授权

- 决定：`hub_handoff` 和 transcript 只提供未信任历史；当前目标、范围和验收以仓库合同为准。
- 原因：聊天越长，错误继承和已否决方案复活的风险越高。
- 重评条件：不重评这一安全边界；未来产品最多展示合同，不替用户自动生成授权。

## D-005：Git 交付责任必须写进任务包

- 决定：每个跨 Agent 任务记录 start/end commit、branch、dirty state、commit/push policy、push target 和 integration owner。
- 原因：避免“代码完成但无人 commit/push”，也避免任意 worker 擅自发布。
- 重评条件：只有在替换 Git 作为项目版本系统时重评。

## D-006：并行写入必须隔离

- 决定：共享 worktree 中的写任务串行执行；并行任务必须使用已验证的独立 worktree。
- 原因：多个 Agent 在同一目录会互相吞改动、误判 dirty state，并增加集成错误。
- 重评条件：出现可靠的事务型文件隔离机制。

## D-007：推送不等于发布

- 决定：开发分支 push、合并 `main`、创建 Release、自动对外发内容是四种独立授权。
- 原因：Git 凭据可用不代表用户批准公开发布。
- 重评条件：不重评；只在具体任务中逐项授权。

## D-008：先用协议狗粮，再考虑编排产品

- 决定：先真实运行 3～5 次仓库合同接力；暂不开发 Manager Agent 或 Hub 调度 API。
- 原因：当前主要摩擦来自状态和交付责任不清，而不是缺少调度系统。
- 重评条件：多次实跑反复出现同一种、无法靠合同解决的摩擦。
