# 路线图与开发节奏

## 当前状态：路线图冻结 · 打磨期（2026-08-08 起）

功能密度已达临界点，**暂停新增功能**，进入自用打磨期：

- 以真实使用为主，记录摩擦点（界面拥挤、流程多点一步、体验别扭处）
- 打磨期产出的问题清单，作为解冻后迭代的输入
- 解冻原则：每个新功能须由真实使用中的摩擦点驱动，而非"想到就加"

## 路线图（冻结中，仅存档）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P0** | Agent 接入层：`/agent/*` 只读 API + `hub_agent.py`（CLI/MCP 双模式） | ✅ 已交付（v0.1.7） |
| **P1** | 对话预蒸馏摘要（规则版优先；模型 API 做成可选增强，默认关闭、不填 key 即不存在） | ⏸ 冻结 |
| **P1** | Context Packet 上下文接力包（跨 agent 交接） | ⏸ 冻结 |
| **P2** | 记忆卡片 + 定期蒸馏（上下文/记忆工程） | ⏸ 冻结 |
| **P3** | Agent 资产索引（PPT/文档等产物管理） | ⏸ 冻结 |

## 设计底线（解冻后依然有效）

- 本地优先：只绑定 127.0.0.1，原始数据只读
- 零第三方依赖：纯 Python 标准库
- 模型永远是可选增强，不是依赖；默认路径离线可用
