---
task_id: T-003
status: review
owner_role: primary Codex integrator
updated: 2026-08-21
---

# T-003: 修复打包漏项并交付双安装方案

## Goal

发布可放心宣发的 `v0.4.1`：Windows/macOS 成品包含 Grok/Qoder 共享恢复层，并同时提供 Agent 一句话安装与 GitHub Release 下载安装，两条路径都会自动发现本机数据源并向用户的 Agent 写明安装路径和使用方法。

## Scope

- 修复 PyInstaller 对 `agent_recovery` 动态模块的漏包，并移除尚未接入插件的误导性宣传。
- 为 Windows/macOS 构建后成品增加启动、健康、数据源与 Grok/Qoder 模块冒烟测试。
- 扩展 Agent 安装器：自动发现路径、安装两个 Skill、安全登记 Codex/Grok MCP、生成本机 `AGENT_USAGE.md`。
- 增加一份可通过 URL 交给 Agent 执行的一句话安装协议，兼容有/无 Git 的 Windows/macOS 环境。
- 让 Release 下载版首次启动也自动写入数据源配置和 Agent 使用说明，并在压缩包中附带接入脚本/说明。
- 修正文档中的版本、适配器数量、插件状态和双安装入口。
- 验证、提交、创建 PR、通过三平台 CI、合并 `main` 并发布 `v0.4.1`。

## Do not touch

- 不写入、移动或迁移任何厂商会话数据库；只探测并保存路径。
- 不覆盖用户已有 Skill 以外的文件，不删除 Agent 配置，不写入凭据。
- MCP 配置只能幂等更新 `conversation-hub` 自己的区块；不能破坏其它 MCP。
- 不启用未接入的腾讯记忆插件，不对外开放 localhost 服务。
- 不 force-push，不改写 `v0.4.0`，不发布 `v0.4.1` 以外的版本。

## Inputs

- 任务起点：`main` at `1c92c7a58cc5d2f902927fba1749bda7ce666889`，工作区干净。
- 用户于 2026-08-21 明确要求直接修复，并交付 Agent 一句话安装和 Release 安装包，均兼容 Windows/macOS。
- 审计确认 v0.4.0 成品归档缺少 `agent_recovery`；源码移除 Skill 路径后 Qoder/Grok 均触发 `ModuleNotFoundError`。
- v0.4.0 源码、本地 `main` 与 GitHub `main` 已同步，Grok 相关提交均已整合。

## Acceptance criteria

- [ ] Windows/macOS PyInstaller 成品包含并能导入 `agent_recovery.qoder` 与 `agent_recovery.grok`。
- [ ] Release workflow 在打包后启动最终成品，健康检查、14 数据源枚举及 Grok/Qoder 模块检查通过后才允许发布。
- [ ] Agent 安装器在 Windows/macOS 上自动发现并原子写入有效路径，保留既有配置，幂等安装 `conversation-hub` 与 `find-agent-data`。
- [x] 安装器只更新自己的 Codex/Grok MCP 配置，并生成含真实绝对路径、CLI/MCP 示例和隐私边界的 `AGENT_USAGE.md`。
- [x] 用户可把 README 中的一句话和公开安装协议 URL 交给具备 shell/文件权限的 Agent，由 Agent 完成 clone/下载、安装、验证与结果汇报。
- [ ] Release ZIP 包含普通用户启动文件、Agent 接入脚本和说明；Windows/macOS 文案准确区分两种安装方式。
- [x] README 中版本、14 个适配器、插件未接入状态和平台限制一致。
- [x] 单元测试、Markdown、CI smoke、安装器隔离测试、合同/Skill 校验与 `git diff --check` 全部通过。
- [ ] PR 与合并后 `main` 的 Windows/macOS/Linux CI 全部通过。
- [ ] `v0.4.1` Release 成功，Windows/macOS 两个资产完成成品冒烟并可下载。

## Git delivery

- repo_root: .
- base_branch: origin/main
- start_commit: 1c92c7a58cc5d2f902927fba1749bda7ce666889
- work_branch: codex/release-v0.4.1-installers
- commit_policy: required
- push_policy: allowed
- push_target: origin/codex/release-v0.4.1-installers, PR to main, origin/main, tag v0.4.1
- integration_owner: primary Codex integrator
- end_commit: pending
- dirty_state: clean at task start

## Release authorization

- merge_policy: allowed
- merge_target: main
- tag_policy: allowed
- tag_target: v0.4.1
- release_policy: allowed
- release_target: GitHub Release v0.4.1 with tested Windows and macOS installers

## Attempts and evidence

- 2026-08-21：建立 `codex/release-v0.4.1-installers` 分支并激活本任务。
- 2026-08-21：新增统一 `agent_setup.py`、独立控制台 `AIConversationHubAgent` 入口、Windows/macOS Agent 接入包装脚本及公开 `AGENT_INSTALL.md`。
- 2026-08-21：安装器隔离测试验证两次运行幂等、保留其它 MCP、安装 2 个 Skill × 3 个 Agent 目录并生成 `AGENT_USAGE.md`；Skill 更新改为相同内容跳过、不同文件原子替换。
- 2026-08-21：修复 `ci_smoke.py` 固定 8765 可能误连用户实例的问题，改为动态空闲端口并核验隔离数据目录。
- 2026-08-21：53 个 Python 单元测试、Markdown 渲染、14 数据源 CI smoke、合同/Skill 校验、workflow YAML 解析和 `git diff --check` 通过。
- 2026-08-21：按 Release 等价参数完成本地 Windows 双 EXE 构建；最终桌面成品实际启动并通过 0.4.1 健康、14 数据源、Grok/Qoder 恢复、Agent setup、CLI ping 与 MCP initialize/tools/list 冒烟。
- 2026-08-21：真实本机安装狗粮通过；在 `%LOCALAPPDATA%/AIConversationHub/AGENT_USAGE.md` 生成本机说明，Codex/Grok 各只有一个 conversation-hub MCP 区块，14 个来源均发现为有效。

## Files changed

- 产品与 Agent 接入：`agent_setup.py`、`agent_cli.py`、`desktop_app.py`、`hub_agent.py`、`scripts/install_agent_access.py`。
- 打包与验收：`.github/workflows/ci.yml`、`.github/workflows/release.yml`、`scripts/frozen_smoke.py`、`scripts/ci_smoke.py`、`tests/test_agent_setup.py`。
- 用户入口：`AGENT_INSTALL.md`、Windows/macOS Agent 包装脚本、安装说明、中英文 README。
- 版本与合同：`server.py`、`launcher.py`、`PROJECT.md`、`DECISIONS.md`、`TASKS.md`、`ROADMAP.md`、本 handoff。

## Current status

本地实现、隔离测试、真实安装狗粮和 Windows 最终成品冒烟均完成；任务进入 GitHub PR 与 macOS/三平台交付审查阶段。

## Next step

提交并推送开发分支，创建 PR 并等待 GitHub 三平台 CI；由 primary Codex integrator 执行。
