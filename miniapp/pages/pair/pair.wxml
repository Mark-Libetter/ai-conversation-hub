<view class="container">
  <view class="card">
    <view class="card-title">配对到电脑</view>
    <view class="muted">
      1. 电脑与手机连同一 Wi-Fi。\n
      2. 在电脑浏览器打开 http://127.0.0.1:8765 ，于「设置」开启伴随端并复制配对码（或直接访问 /api/companion 查看 IP 与配对码）。\n
      3. 在下方填写电脑 IP、端口与配对码，保存后点「测试连接」。
    </view>
  </view>

  <view class="card">
    <input class="input" placeholder="电脑局域网 IP，如 192.168.1.10" value="{{ip}}" data-field="ip" bindinput="set" />
    <input class="input" placeholder="端口（默认 8766）" type="number" value="{{port}}" data-field="port" bindinput="set" />
    <input class="input" placeholder="配对码（token）" value="{{token}}" data-field="token" bindinput="set" />
    <button class="btn" bindtap="test" disabled="{{testing}}">{{testing ? '测试中…' : '测试连接'}}</button>
    <button class="btn ghost" bindtap="save">仅保存</button>
  </view>

  <view class="card" wx:if="{{status}}">
    <view class="{{connected ? 'item-title' : 'item-text'}}" style="color:{{connected ? '#2d927d' : '#b0483f'}}">{{status}}</view>
  </view>

  <view class="card">
    <view class="muted">隐私说明：伴随端只读取电脑已脱敏的日报/待办摘要，原始对话不会离开电脑；配对码仅用于局域网鉴权，可随时在电脑端更换。</view>
  </view>
</view>
